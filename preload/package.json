{
  "name": "@bdbrowser/preload",
  "description": "BDBrowser Preload",
  "version": "0.0.0",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "build": "webpack --progress --color",
    "build-prod": "webpack --stats minimal --mode production",
    "lint": "eslint --ext .js src/"
  }
}
